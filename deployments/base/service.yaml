apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app: jwtinspect
  name: jwtinspect
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: jwtinspect

